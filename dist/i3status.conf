general {
    output_format = "i3bar"
    interval = 1
    colors = true
}

order += "load"
order += "tztime local"

load {
    format = "load %1min"
}

tztime local {
    format = "%Y-%m-%d %H:%M"
}
